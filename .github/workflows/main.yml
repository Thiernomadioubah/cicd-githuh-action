name: Déploiement

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: https://github.com

    steps:
      - name: deploiement
        # ...
